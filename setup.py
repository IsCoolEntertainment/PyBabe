#!/usr/bin/env python

from setuptools import setup

setup(
    name='PyBabe',
    version='1.0',
    packages=['pybabe'],
    install_requires=[],
    tests_require=[
        'stubserver==0.2'
    ],
)
